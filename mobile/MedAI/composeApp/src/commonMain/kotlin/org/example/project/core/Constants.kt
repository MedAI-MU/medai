package org.example.project.core

object Constants {
    // Note: Assuming emulator usage locally, so 10.0.2.2 points to host's localhost
    // Real deployment would use an actual HTTPS backend endpoint
    const val BASE_URL = "http://10.0.2.2:3000"
}
